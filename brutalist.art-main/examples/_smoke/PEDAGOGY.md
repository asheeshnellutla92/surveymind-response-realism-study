# PEDAGOGY AUDIT — _smoke

This is the fixture reel for `./art smoke` (`runtime/scripts/smoke_test.sh`),
not a real explainer. It exists only to exercise Kokoro synthesis, `run.sh`
(GATE L, the zero-Manim skip path, the Remotion fill-in pass) and
`compile.py` end to end on a real render, so GATE P is pre-approved here.

VERDICT: PASS
